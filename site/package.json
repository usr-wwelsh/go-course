{
  "name": "go-course-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for the Go, by Doing course",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "serve": "python3 -m http.server 8080 --directory dist"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@codemirror/lang-go": "^6.0.1",
    "codemirror": "^6.0.2"
  }
}
